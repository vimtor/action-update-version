name: Publish
on:
  release:
    types: [published]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - uses: ./
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
