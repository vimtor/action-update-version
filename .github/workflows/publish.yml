name: Publish
on:
  release:
    types: [published]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - uses: ./
        with:
          version-regexp: 'v\d'
          repo-token: ${{ secrets.GITHUB_TOKEN }}
